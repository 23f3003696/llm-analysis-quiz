# LLM Analysis Quiz

FastAPI app for handling IITM LLM Analysis Quiz submission.

## Usage
1. `pip install -r requirements.txt`
2. `uvicorn main:app --reload`
3. POST to `/quiz` with JSON containing email, secret, url.

